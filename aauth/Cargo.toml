[package]
name = "aauth"
version = "0.0.3"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
description = "Rust implementation of the AAuth authorization protocol"
documentation = "https://docs.rs/aauth"
readme = "../README.md"
keywords = ["aauth", "oauth", "authentication", "http-signatures"]
categories = ["authentication", "web-programming"]

[features]
default = [
    "agent",
    "agent-reqwest",
    "agent-reqwest-verify",
    "person-server",
    "access-server",
    "resource",
    "person-server-axum",
    "access-server-axum",
    "resource-axum",
]

# --- Agent ---
agent = []
agent-reqwest = [
    "agent",
    "dep:reqwest",
    "dep:reqwest-middleware",
    "dep:http-body-util",
    "dep:tokio",
]
agent-reqwest-verify = ["agent-reqwest", "resource-verify"]

# --- Shared server infrastructure ---
deferred = []
policy = ["deferred"]
deferred-http = ["deferred", "dep:reqwest", "dep:tokio"]

# --- Resource token verification (no RS service/layer) ---
resource-verify = []

# --- Protocol roles ---
person-server = [
    "agent",
    "deferred",
    "policy",
    "resource-verify",
    "deferred-http",
]
access-server = ["deferred", "policy"]
resource = ["deferred", "policy", "resource-verify"]

# --- Axum integration (per role) ---
server-axum = ["dep:axum", "dep:tower"]
person-server-axum = ["person-server", "server-axum"]
access-server-axum = ["access-server", "server-axum"]
resource-axum = ["resource", "server-axum"]

# --- Convenience meta-features ---
server = ["person-server", "access-server", "resource"]
full = [
    "agent",
    "agent-reqwest",
    "agent-reqwest-verify",
    "server",
    "person-server-axum",
    "access-server-axum",
    "resource-axum",
]

[dependencies]
async-trait.workspace = true
axum = { workspace = true, optional = true }
tower = { workspace = true, optional = true }
base64.workspace = true
bytes.workspace = true
ed25519-dalek.workspace = true
http.workspace = true
jsonwebtoken.workspace = true
rand.workspace = true
anyhow.workspace = true
reqwest = { workspace = true, optional = true }
reqwest-middleware = { workspace = true, optional = true }
http-body-util = { workspace = true, optional = true }
serde.workspace = true
serde_json.workspace = true
serde_with.workspace = true
sfv.workspace = true
sha2.workspace = true
thiserror.workspace = true
tokio = { workspace = true, optional = true, features = ["time"] }
url.workspace = true
uuid.workspace = true

[dev-dependencies]
tokio.workspace = true
wiremock.workspace = true
reqwest.workspace = true
reqwest-middleware.workspace = true
http-body-util.workspace = true
anyhow.workspace = true
axum.workspace = true
tower.workspace = true
async-trait.workspace = true
serde_json.workspace = true
rstest.workspace = true

[[example]]
name = "identity_based"
required-features = [
    "agent-reqwest",
    "person-server",
    "access-server",
    "resource",
    "person-server-axum",
    "access-server-axum",
    "resource-axum",
]

[[example]]
name = "person_server_managed"
required-features = [
    "agent-reqwest",
    "person-server",
    "access-server",
    "resource",
    "person-server-axum",
    "access-server-axum",
    "resource-axum",
]

[[example]]
name = "resource_managed"
required-features = [
    "agent-reqwest",
    "person-server",
    "access-server",
    "resource",
    "person-server-axum",
    "access-server-axum",
    "resource-axum",
]

[[example]]
name = "federated"
required-features = [
    "agent-reqwest",
    "person-server",
    "access-server",
    "resource",
    "person-server-axum",
    "access-server-axum",
    "resource-axum",
]

[[test]]
name = "axum_integration"
required-features = ["full"]

[[test]]
name = "example_flows"
required-features = ["full"]

[package.metadata.docs.rs]
all-features = true
